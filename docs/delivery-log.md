# Delivery Log

| Plan | Pull request | Implementation rounds | Review dispositions | Drift cause |
| --- | --- | ---: | --- | --- |
| evidence-pipeline-warning | none — this repository has no remote | 1 | ACCEPT | Prose had already failed three times, so the reader now marks a standalone pipe itself: a rule asking a worker to be careful with a value loses to a mechanism that supplies it. Discovered in-plan: the focused test must invoke the reader through bash because the repository ships no executable-bit rail for tests/; the plan file was committed with an empty Baseline field; and the read-only Codex reviewer reproduced only three of the four gates, since the focused test's mktemp was denied by the sandbox, which also blocked its result file and its worker_done, so the coordinator settled ACCEPT from the reviewer's own terminal and retired the stranded dispatch with the documented task-update recovery rather than fabricating a completion. |
| review-capability-contract | none — this repository has no remote | 2 | REMEDIATE_ONCE → ACCEPT | The capability record promoted an undocumented Codex default and the plan omitted its own post-baseline metadata path; independent review narrowed both without adding a mechanism. |
| pseudonymise-consumer-identifiers | none — remote is being deleted and recreated before any PR | 7 | REMEDIATE_ONCE → REPLAN_OR_SPLIT → REMEDIATE_ONCE (not remediated; control escalated to the product owner) → REMEDIATE_ONCE → ACCEPT | The plan twice specified a disclosure instrument that had not been run against the text it shipped. First, a pattern covering one identifier family out of four, so a green gate left other letter-digit plan labels in the tree. Then a pattern that was measured case-sensitive and anchored but written with case-insensitivity and no anchors, which matched 76 lines where the plan claimed 13: hex SHAs, version strings, and other letter-digit tokens inside larger identifiers. Anchoring and case were the entire mechanism; dropping them while keeping the description certified the opposite of the contract. The gate is two commands because the project name needs case-insensitivity and the identifier family must not. Separately, the contract was scoped by enumerating classes of residue, so each round discovered a class the previous contract had not named — names, then hex identifiers, then pull request numbers and internal filenames — until the product owner reframed the unit as an ownership split, which is the framing that could state when it was done. The third review disposition was not remediated: a third distinct class of residue had appeared, and control escalated to the product owner instead of sending another implement pass. Also discovered in-plan: no lexical rule separates this project's own probe identifiers from the consumer's, so hex ownership is read from the surrounding section rather than grepped, and a UUID or short-hex gate would be permanently red or routinely overridden; the extract of project-owned evidence is cut from the unscrubbed text in history, because pseudonyms protect readers of this package and are noise inside the consumer's own repository; and the pattern was verified only through git grep's engine, so a later run through a different grep must re-check self-match. Standing consequence: an instrument is cited by running it, not by describing it, and the same bytes must appear in the plan, the decision record, and the gate. |
| interactive-workers-and-unsandboxed-review | https://github.com/hieuphung97/dely/pull/1 | 1 | ACCEPT | Control used headless harness commands despite Orca being selected, wrapping `claude -p`, `codex exec` and `grok --prompt-file` in visible shell tabs so no coordinator Run or Task existed, and Codex `--sandbox read-only` review blocked gates, network, result writes and `worker_done`. |
| rename-dely-identity | https://github.com/hieuphung97/dely/pull/2 | 3 | REPLAN_OR_SPLIT → REMEDIATE_ONCE → ACCEPT | Neither cause is the implementers'. First, the plan and the decision record both told the implementer to write `grok plugin install dely@dely`, a Claude Code and Codex marketplace selector that Grok does not share: `grok plugin install` takes a source, and its `@ref` suffix is a git ref, so that string reads there as the repository `dely` at ref `dely`. Design specified a command without checking it against the harness's own `--help`, which is the same class as citing an instrument without running it. Second, the doctor check was specified as "the referenced root exists and is usable" without saying *every* referenced root and *both* hook scripts, and the first implementation read only the first command; the remediation then skipped empty commands. Both produced a `delivery-doctor` that reported `ok` on an adapter that journals nothing — reports-as-fine-and-is-not, in the one program whose job is to catch exactly that, and each was found only because a reviewer built a fixture rather than reading the diff. Also discovered in-plan: the acceptance row for "the adapter root is derived, not typed" originally had no instrument and was marked for a human to read; a human read it and found the defect, and the row now has a grep. An acceptance row with no instrument is worth keeping only when someone actually reads it. |
| routine-grok-adapter-command-target | https://github.com/hieuphung97/dely/pull/2 | 2 | REMEDIATE_ONCE → ACCEPT | The previous unit's check validated the adapter's *root* and then, after remediation, every *present* command — but never what a command actually called, nor whether all three required events were registered. An adapter whose commands all named a nonexistent script beside the two real ones passed, and an adapter with `PostToolUseFailure` deleted passed. Both were found by a monitor or a reviewer building a fixture, never by reading the diff. The check has now been corrected four times across this branch, and each correction moved the assertion closer to the command itself. |
| setup-skill-and-managed-block | https://github.com/hieuphung97/dely/pull/3 | 2 | REMEDIATE_ONCE → ACCEPT | The first implementation's `bin/delivery-doctor` accepted a managed block whose phase rows had empty Harness, Model and Effort cells, and the new focused test passed it. The parser read only each row's first cell and never checked the remaining cells, their count, or row uniqueness. Independent review found it by copying the package and blanking the cells; Control reproduced it before ordering remediation. This is the same shape this repository has now corrected repeatedly: a check that asserts the surroundings rather than the thing itself. Two acceptance rows were present and run and still proved nothing. The marker-bounded-read case claimed to prove the doctor is bounded by the markers, but its inner block was valid so a whole-file grep passed it too. The catalogue-absence case claimed to prove the absence of catalogue validation, but the literal `default` would also be accepted by a validator whose catalogue contained it. Both were fixed to use an incomplete inner block with the missing row only outside, and novel non-catalogue values. Standing consequence: an acceptance row is not discriminating because it was run, only because a counterexample was tried against it. Discovered in-plan by Control, and recorded here because the plan file was its only home: `orca orchestration check --wait --json` streams `{"_keepalive":true,...}` objects on their own lines before the final pretty-printed result. A client that pipes the stream into a single `json.load` dies on the second line. That killed Control's first blocking waiter, while the Orca runtime still reported `waiter_exists` and rejected two later waits, so an already-sent `worker_done` sat undelivered until Control checked the Run directly. Parse the stream line by line and ignore keepalives, or write it to a file and strip them before parsing. Worth promoting into `docs/findings.md` by a later observation-owned unit. Also recorded: Orca refuses launch-time model selection for Grok, so both Grok workers and the Codex reviewer were launched with the model, effort and sandbox baked into the terminal argv and then attached with `worker-start --terminal`. That works, and the cost is that `launch.requested` and `launch.effective` both come back `null`, so Orca's provenance does not record the pin. Control verified the pins out of band with `ps -Ao args`. |
| counterexample-column | https://github.com/hieuphung97/dely/pull/7 | 1 | ACCEPT | The unit exists because the rule that an acceptance instrument must discriminate had already failed nine times, most recently in this repository's own `setup-skill-and-managed-block` plan. The instrument there was observed red at baseline for the right reason, and the journal proves it, but it was red because the feature did not exist: every check of an absent feature is red, so baseline-red is satisfied trivially and says nothing about detecting an implementation that is present and wrong. The acceptance table now carries a Counterexample column filled at design and an Observed red column filled at implement, on the same principle that makes `END OF HANDOFF` load-bearing — absence is detectable, prose is not. Recorded by Control during this plan, because the plan file was its only home: the first review dispatch under the new `AGENTS.md` pin confirmed that Grok accepts `xhigh` for `grok-4.6`. It could not be verified before dispatch — `grok --reasoning-effort bogus models` exits 0 without validating — so the pin shipped unverified by design and the first real dispatch was the check. It passed: the prompt submitted and the model responded with no unknown-effort error. Also recorded: Control's own verification of a counterexample initially read its exit code through a pipe, so `$?` reported `tail` rather than the check and showed `0` for a check that had actually failed. The skill already warns about exactly this, and it still happened while verifying a unit about verification. |
| coordinator-not-offered | https://github.com/hieuphung97/dely/pull/8 | 1 | ACCEPT | The defect was an undefined branch, not a broken rule. Setup already said to offer Orca where available and had no rule for being unable to offer at all, so a non-interactive run wrote `Coordinator: none` silently while Orca was installed and running. `AGENTS.md` cannot afterwards tell `none` chosen by a human from `none` defaulted by a run that could not ask. A second item was investigated and rejected: `findings.md` §32 predicted a collision between this skill and `codex:setup`. That is a command, not a skill; the Codex plugin ships three skills and none is named `setup`; and a collision on a harness that flattens both namespaces resolves by qualification, which is already the canonical invocation. §32 was corrected in place rather than deleted. Recording a prediction that dissolved is worth as much as recording one that held. Recorded by Control during this plan, because the plan file was its only home: the `input_accepted is not input_submitted` race from `findings.md` §23 reproduced on the implement dispatch. The prompt sat in the input box as a pasted block with no heartbeat and no file changes for roughly nine minutes until one `orca terminal send --enter` released it. §23 concluded the race did not reproduce when the terminal already existed; here the terminal was created explicitly, waited to `tui-idle`, and attached with `worker-start --terminal`, and it still reproduced. §23's scoping is too narrow, and a dispatch should be checked for a pending unsubmitted prompt rather than assumed submitted. Also recorded: Control's own attempt to reproduce the acceptance counterexample was invalid twice before it was valid. First it passed a root argument to `tests/managed-block-contract.sh`, which derives its root from its own location and ignores arguments, so the run silently tested the real repository and passed. Then it used a partial copy, which failed for unrelated missing files. Only a complete copy with the test run from inside it produced the real result. Both errors are the same class the unit itself is about — a check that appears to exercise something and does not. |
| setup-offers-claude-import | https://github.com/hieuphung97/dely/pull/10 | 2 | REPLAN_OR_SPLIT → ACCEPT | Report-only was too weak, and this repository was the evidence. That Claude Code cannot read `AGENTS.md` had already been written into a decision record, written again into `findings.md` §32, and printed by the skill during the forward smoke — three times — and the repository whose control phase is Claude Code still had no `CLAUDE.md` until a human asked why. A report everyone reads and nobody acts on is indistinguishable from silence. The plan also added no non-interactive rule for the new offer, betting that the general rule shipped in `0.8.0` already covered it. Independent review disproved the bet and returned `REPLAN_OR_SPLIT`. That rule's nouns and verbs all belonged to a value written into the managed block, it sat under the `Coordinator` heading, and the test enforcing it read only that section. Against an offer to create a file it did not merely fail to apply: read literally, "write the conservative value" could be taken as licence to create `CLAUDE.md` unasked, the opposite of "never write it unasked" two paragraphs later. A gap that inverts under a literal reading is worse than an absent rule. The correction was structural rather than verbal — the rule moved into its own section with action-neutral verbs, with the coordinator and the import as instances. The standing lesson: `0.8.0` was written to be general and its own decision record called it general, but it was placed under a domain-specific heading with value-shaped verbs and its test only looked there. Saying a rule is general does not make it general; where it lives and what its test reads do. Recorded by Control, because the plan file was its only home: the `input_accepted` race from `findings.md` §23 reproduced again, and Control's own 25-second terminal check reported "submitted" when the prompt was in fact still pending, because it looked for a rendered line that had not appeared yet. The reliable signal is the dispatch heartbeat: absent after 90 seconds means the prompt is sitting unsubmitted. Across this plan the race hit two of the dispatches, and both were released by a single `orca terminal send --enter`. |
| dispatch-submission-recovery | pending — not yet pushed | 1 | ACCEPT | The portable contract treated coordinator `input_accepted` as the start of the long wait, and findings §23 scoped the unsubmitted-draft race to a freshly launched terminal. Three later deliveries reproduced the same idle draft on an existing terminal; a 25-second visual check of a rendered prompt called it submitted when it was still pending. Absence of a dispatch heartbeat after 90 seconds, a TUI read that shows the prompt still in the input box, and one Enter is the discriminating recovery; trusting the receipt, skipping the read, or repeating Enter are the failures the focused check now rejects. Also recorded, because the plan file was its only home: Orca still has no native `input_submitted` receipt, so the deferred trigger in the decision stands; and a 90-second no-signal window against a five-minute worker heartbeat means Control will often take the TUI-read path, which is accepted — Enter still fires only when the prompt is pending. |
